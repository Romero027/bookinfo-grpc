syntax = "proto3";
option go_package = "./proto/reviews";

package reviews;

service Ratings {
    rpc getRatings(Product) returns (Result);
}

message Product {
    string id = 1;
}

message Result {
    string ratings = 1;
}