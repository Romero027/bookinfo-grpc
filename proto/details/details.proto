syntax = "proto3";
option go_package = "./proto/details";

package details;

service Details {
    rpc getDetails(Product) returns (Result);
}

message Product {
    string id = 1;
}

message Result {
    repeated string details = 1;
}