syntax = "proto3";
option go_package = "./proto/reviews";

package reviews;

service Reviews {
    rpc getReviews(Product) returns (Result);
}

message Product {
    string id = 1;
}

message Result {
    repeated string reviews = 1;
}